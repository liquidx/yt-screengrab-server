This is a server that uses yt-dlp to download a screengrab from a YouTube live stream.

- Keep the code simple.
- Use uv to run python in a virtual environment.
- The server is intended to run in a Docker container.
- The server should be run under linux.
- The server code should be in server/
